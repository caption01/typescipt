const drink = {
  color: "brown",
  carbonated: true,
  sugar: 40,
};

//type alias
type Drink = [string, boolean, number];

const pepsi: Drink = ["brown", true, 40];
const sprite: Drink = ["white", true, 80];

const carSpecs: [number, number] = [400, 3353];

const carStates = {
  horsePowers: 400,
  weight: 3551,
};
